class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int n;

        for(int i = 0; i < n; i++){
            cin >> n;
            nums.push_back(n);
            cout << nums[i];
        }
    }
};