class Solution {
public:
    
        int x;
        cin >> x;
        vector<int> nums;
        int val;
        for(int i = 0; i < nums.size(x)); i++){
            cin >> val;
            nums.push_back(val);
            cout << nums[i];
        }
    }
};
//Code is not finished
