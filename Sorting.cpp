#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<int> x;
    int x;
    for(int i = 0; i < n; i++){
        cin x;
        x.push_back(x);
    }
    sort(x.begin(), x.end());
    unique(x.begin(), x.end());
    for(int j: v){
        cout << v[i];
    }
    
}
 